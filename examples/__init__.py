"""Runnable examples for langchain-dynamic-workflow.

This package exists only so the demos can run as modules
(``python -m examples.<sub>.<name>``) with absolute imports; it is not part of
the published wheel.
"""
